This folder (and git) publishes to the connect.triquence.org app at Heroku and
also the https://github.com/m-jim-d/springsandpucks-nodejs-server.git repository at GitHub.

To publish, run: 
publish.bat

To run locally in development mode, run:
run-server.bat dev

To run in production mode, run:
run-server.bat prod

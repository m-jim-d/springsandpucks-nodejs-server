This folder (and git) publishes to the connect.triquence.org app at Heroku.

To publish, run: 
publish.bat

To run locally in development mode, run:
node universal-server.js

To run in production mode, run:
run-prod.bat
